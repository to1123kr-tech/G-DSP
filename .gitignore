# ── 서버에서만 만들어지는 파일 (깃에 올리지 않음) ──
gdsp_admin_pw.txt      # 관리자 비밀번호
gdsp_inq.json          # 문의 게시판 데이터
gdsp_inq.json.tmp
gdsp_visits.json       # 방문 통계
gdsp_visits.json.tmp
gdsp_licenses.json     # 발급한 락키 목록  ← 고객 정보
gdsp_licenses.json.tmp
gdsp_lic_secret.txt    # 락키 응답 서명용 비밀값
gdsp_vworld_key.txt    # VWorld 인증키 (6개월마다 바뀜 · 여기 한 곳만 고치면 됨)

# ── 파이썬 ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 로그 · 임시 ──
*.log
nohup.out
log.txt
*.bak
*.tmp

# ── OS ──
.DS_Store
Thumbs.db
